# Implementation Plans — Memory View Improvement

Generated by the improve + improve-animations skills on 2026-07-13,
Execution 2026-07-13: plans 001-005 DONE on branch `advisor/memory-view-plans`
(worktree; commits stamped per row). 006-012 remain TODO by the user's
decision (token budget) — run individually later; drift checks will flag the
001-005 changes, which is expected.
 against
commit `57ec2d10` (branch `codex/memory-ledger-v2`, dirty working tree — all
excerpts were verified against the working tree, not HEAD).

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done. Repo verification gate (from `apps/desktop/`):
`bun run typecheck && bun run lint && bun test tests/` (+ `bun run build` for
shell-level changes). Do NOT commit or push without the operator's say-so.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix nonexistent `warning`/`danger` color classes | P1 | S | — | DONE (478cff00) |
| 002 | Remove `mode="wait"` dead gap on note navigation | P1 | S | — | DONE (ad42c7f7) |
| 003 | Quick fixes: honest Refresh, bounded draft store | P1 | S | — | DONE (f7929f07; indexBlocked step REJECTED — by-design, see plan) |
| 004 | Backlinks IA: rename, reorder, auto-open inspector | P1 | M | 001 (soft) | DONE (f2b4d1fc) |
| 005 | Quick switcher (Cmd+O fuzzy note jump) | P1 | M | — | DONE (b48da9ab) |
| 006 | Rail UX: collapsible folders, reveal-active, arrow keys | P2 | M | — | MOSTLY DONE (2026-07-13 redesign, main tree): collapse+reveal+one-line rows shipped; REMAINING: arrow-key roving tabindex |
| 010 | Code health: dedupe conflict/search, records tests | P2 | M | — (before 008) | DONE (2026-07-13, main tree): conflictFromDrift helper ×3 sites, search debounce delegates to shared callback, pin rollback guarded by recordsRequestId + failure→toast (not list-blanking error), 5 records tests |
| 007 | Motion pass: panel/mode/skeleton/rail transitions | P2 | M | 002, 006 | DONE (2026-07-13, main tree) with DEVIATION: mode/skeleton/rail reveals are ENTER-ONLY (keyed rise-in) — exit-based crossfades left exiting panels alive holding focus; Forget-confirm eases in; CopyPath uses BlurSwap; focus reclaim = body-only two-stage retry |
| 008 | Search: include index/file pages, grouping, in-note find | P2 | M | 010 (soft) | DONE (2026-07-13, main tree): resource-wide filter, Notes / Indexes & files groups, Cmd+F find bar (CSS Custom Highlight API, 4 tests) |
| 009 | Design conformance: type scale, dividers, radii, skeletons | P2 | M | 004, 006, 007 | MOSTLY DONE (redesign): text-3xl/text-md tokens, dividers, 10px fix, inspector skeletons, duration-check; REMAINING: radius normalization + ScheduleChip |
| 011 | Promote memory modal → full workspace surface | P2 | L | 001–007 (soft) | DONE (2026-07-13 redesign, main tree): MemorySurface + resizable panes + last-path restore; MemoryModal deleted |
| 012 | Design spike: note authoring (create/rename/delete) | P3 | L | — (design work) | TODO — deliberately remaining: server-side design doc + POC, not UI; run when server work is next on deck |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **010 before 008**: 010 unifies the duplicated search fetch paths; 008
  edits the search filter. Running 008 first means editing two sites and a
  drift-check failure for 010.
- **002 and 006 before 007**: 007's motion wrappers assume TabPanels is
  already fixed (002) and wraps rail markup that 006 restructures.
- **004/006/007 before 009**: 009 restyles class strings on the same lines
  those plans touch; running it last avoids rebase churn.
- **011 last of the build plans**: it relocates the whole view; smaller
  diffs should land first. It also unlocks 004's deferred right-panel tabs.
- 001, 003, 005 are independent — safe to parallelize.

## Findings considered and rejected / deferred (do not re-audit)

- **Edit/review reducer refactor + god-component split** (ArtifactMemoryView
  is 1559 lines, 8 interdependent edit states): HIGH-value but L-effort/HIGH-
  risk while the branch churns; behavior is pinned by ~34 tests. Revisit
  after a churn lull; plan 010's coverage makes it safer. Concurrency
  correctness itself was audited SOLID (generations/AbortControllers used
  consistently; pinned by tests) — audit the shape, not the race safety.
- **`useAsyncResource` consolidation of all 8 fetch sites**: same deferral;
  only the search pair is unified (plan 010).
- **Graph view**: most-cited Obsidian feature, but L-effort with no backing
  data assembly server-side (`scopes` endpoint explicitly "not a graph
  browser"). Behind 005/004 in leverage.
- **Wikilink hover-preview fidelity** (render real markdown + metadata
  instead of first-paragraph text): real gap, S–M effort, not selected this
  round.
- **Breadcrumb/location trail** above the note title: MED-confidence nicety;
  revisit after 011 (a workspace header is the natural home).
- **Instrument-ruler treatment for the Evidence timeline** (design-language
  "candidate" idiom, RunRuler exemplar in automations): taste spike, LOW
  confidence; needs in-app prototyping, not an executor plan.
- **Record-list paging stagger** (all rows RISE_IN simultaneously on page
  swap): real but LOW; superseded by 007's decision to keep dense-list
  motion minimal — revisit only if paging feels heavy after 007.
- **Editor mode toggle onto the shared Tabs primitive**: LOW confidence the
  primitive fits a 2-state icon toggle; not worth the editor-state risk now.
- **Inspector per-row entrance motion**: rejected in 007 — panel-level
  reveal covers arrival; per-row springs in a dense provenance list would be
  decoration.
- **`ResizeObserver` width-branch tests for MemoryEditReview**: cosmetic
  coverage gap, LOW; fold into any future work touching that component.
- **`reviewPending` listed as an unused dep in the default-selection effect**
  (`ArtifactMemoryView.tsx:456`): MED-confidence micro-smell; fix
  opportunistically inside plan 010's file edits if trivially safe, else
  leave.
- **Real index rebuild endpoint**: memory is file-canonical by documented
  design; the honest fix is labeling (plan 003), not a new endpoint.
| 013 | memory-draft-port | BLOCKED (awaiting user approval of docs/mockups/memory-workspace-draft.html) |
