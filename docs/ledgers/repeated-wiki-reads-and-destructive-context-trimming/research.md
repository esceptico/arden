# Research

## Surface research

- **Scope**: Canonical versus request-only message trimming; durable tool-result recovery; unchanged resource-read semantics; repeated-successful-call handling; compaction/read-receipt interaction.
- **Sources inspected**: Arden runtime source and persisted live transcript at revision `9090f69f96e5b75312543d3217c9a7e665d44fa0`.
- **Observations**: The limiter returns a copied list, but `Agent._prepare` clears and replaces the canonical `messages` list with that request view. Checkpoint persistence then saves the replaced list.
- **Negative evidence**: The run had no compaction and no wiki mutation before the 14 reads; therefore neither event explains the initial repetition.

## Consolidated findings

| ID | Type | Claim | Evidence | Implication | Confidence | Last checked |
| --- | --- | --- | --- | --- | --- | --- |
| F-01 | fact | Model-context limiting can destructively replace canonical run history. | `apps/server/arden/core/model_context_budget.py:19-27`; `apps/server/arden/agent/agent.py:489-510`; `apps/server/arden/services/chat.py:687-693` | Request-only optimization corrupts persisted evidence and future model context. | high | 2026-08-04 |
| F-02 | fact | A compacted placeholder is not recognized as already compacted. | `apps/server/arden/core/model_context_budget.py:37-76` | Repeated passes produce the observed `61 chars, 1 line` self-description. | high | 2026-08-04 |
| F-03 | fact | Recovery is present only when `find_result_file(tool_call_id)` finds an offloaded blob. | `apps/server/arden/core/model_context_budget.py:68-75` | Inline wiki content can become unrecoverable after destructive trimming. | high | 2026-08-04 |
| F-04 | fact | The no-progress counter only advances for all-nonretryable-failure steps. | `apps/server/arden/agent/agent.py:453-463` | Repeated successful reads are intentionally invisible to the generic failure fuse. | high | 2026-08-04 |
| F-05 | observation | Persisted messages in session `20260720_094827_005` contain many exact `[Older tool result cleared from context — 61 chars, 1 lines.]` values. | `sqlite3 -readonly /Users/escept1co/.arden/sessions.db` query over `session_messages`, observed 2026-08-04 | Confirms repeated placeholder compaction occurred in production data. | high | 2026-08-04 |
| F-06 | fact | Claude Code trims copied request history, persists large results with recovery paths, returns an unchanged-read stub, and clears/rebuilds file receipts on full compaction. | `/Users/escept1co/src/claude-code-leaked/src/query.ts:365`; `src/services/compact/microCompact.ts:295-303,446-492`; `src/utils/toolResultStorage.ts:130-198`; `src/tools/FileReadTool/FileReadTool.ts:523-568`; `src/services/compact/compact.ts:517-539,1398-1448` | Canonical/request separation plus visibility-aware receipts is the reference behavior. | high | 2026-08-04 |
| F-07 | fact | Hermes keeps request selection non-mutating, returns one unchanged-read stub, blocks the third identical visible read, and clears dedup state after full compression. | `/Users/escept1co/src/hermes-agent/agent/conversation_loop.py:839-883`; `tools/file_tools.py:1230-1265,1406-1430`; `tests/tools/test_file_read_guards.py:664-702` | A fuse is viable, but only when dedup state tracks effective visibility. | high | 2026-08-04 |
| F-08 | fact | OpenCode preserves stored outputs while projecting placeholders and has a third-identical-call doom-loop guard; Codex separately persists prepared items and truncates live context, but neither offers semantic read caching. | `/Users/escept1co/src/opencode/packages/opencode/src/session/message-v2.ts:290,521`; `session/processor.ts:29,331`; `/Users/escept1co/src/codex/codex-rs/core/src/context_manager/history.rs:203,344`; `session/mod.rs:2956` | Storage projection and loop protection are separate concerns. | high | 2026-08-04 |
| F-09 | fact | Letta persists compacted active-context IDs while retaining full DB history, but its model recall search excludes tool-result messages. | `/Users/escept1co/src/letta/letta/agents/letta_agent_v3.py:758,800`; `services/message_manager.py:895,939`; `services/tool_executor/core_tool_executor.py:140` | Durable storage alone is insufficient without a model-usable recovery surface. | high | 2026-08-04 |
| F-10 | fact | Anthropic context editing is request-only and leaves application history unmodified; OpenAI compaction defines a new canonical model-context projection. Neither provider supplies semantic duplicate-read correctness. | [Anthropic context editing](https://platform.claude.com/docs/en/build-with-claude/context-editing); [OpenAI compaction](https://developers.openai.com/api/docs/guides/compaction), accessed 2026-08-04 | Arden must own recovery refs and visibility-aware read semantics. | high | 2026-08-04 |
| F-11 | fact | Root and child agents both install an aggregate tool-result request limiter even though `ArdenToolExecutor._bound_result_payload` already bounds every normal serialized result at ingestion. | `apps/server/arden/core/factory.py:175-189`; `apps/server/arden/core/spawner.py:641-670`; `apps/server/arden/core/tool_executor.py:194-198,400-431` | The request limiter is redundant for normal execution and can erase useful accumulated small results. | high | 2026-08-04 |
| F-12 | fact | Durable-call recovery bypasses `ArdenToolExecutor` and appends stored content directly as a tool result. | `apps/server/arden/services/chat.py:65-111`; `apps/server/arden/agent/tools/dispatch.py:18-43` | Recovered oversized results need the same ingestion bound before re-entering history. | high | 2026-08-04 |
| F-13 | fact | Scheduled-loop preparation silently keeps only the newest 50 messages after attempting pre-run compaction. | `apps/server/arden/services/chat.py:704-738,782-793`; `apps/server/arden/constants.py:103` | Removing only the tool-result middleware would not make active context append-only. | high | 2026-08-04 |
| F-14 | fact | `session_messages` retains raw pre-compaction rows, while `load_session` trusts the mutable `sessions.messages` snapshot and does not reconstruct from checkpoints. | `apps/server/arden/context/store.py:1777-1828,4280-4320`; `apps/server/tests/test_session_store.py:2348-2370` | The full transcript exists, but resume needs an explicit ordered checkpoint projection. | high | 2026-08-04 |
| F-15 | fact | A compacted handoff records the summarized range but not the ordered retained tail; retained rows may have transcript sequence numbers older than the newly appended summary. | `apps/server/arden/core/compactor.py:314-346`; `apps/server/tests/test_session_store.py:2348-2370` | “Rows after checkpoint” alone is insufficient; the checkpoint must name its retained tail. | high | 2026-08-04 |
| F-16 | fact | Treating raw append order as the normal active view unions concurrent snapshots and freezes the first system prompt; a valid saved projection must remain authoritative. | `apps/server/arden/context/store.py:_active_context_from_transcript_rows`; `apps/server/tests/test_edge_cases.py:test_concurrent_saves`; `apps/server/tests/test_chat_background_merge.py` | Transcript reconstruction is a corruption fallback, not the ordinary load path. | high | 2026-08-05 |
| F-17 | fact | Rewind by raw sequence is wrong after compaction because the active retained tail can precede the newer summary in transcript sequence order. | `apps/server/arden/context/store.py:rewind_session`; `apps/server/tests/test_session_store.py:test_rewind_deletes_exact_active_suffix_and_reconstructs_after_compaction` | Explicit rewind needs CAS, exact message-ID deletion, and a renewed checkpoint in one transaction. | high | 2026-08-05 |
| F-18 | fact | Durable recovery can read the full serialized `ToolResult` envelope; wrapping it as plain content serializes JSON inside JSON, while joining manifests by nullable `run_id` can miss the result entirely. | `apps/server/arden/agent/types/tools.py:from_serialized_payload`; `apps/server/arden/context/store.py:get_tool_result_for_call`; `apps/server/tests/test_tools.py:test_durable_large_result_recovers_without_nested_serialization` | Recovery must decode the envelope, join through the tool-call result ref, then apply the normal ingestion bound. | high | 2026-08-05 |
| F-19 | fact | OpenAI Responses stateless replay requires the complete ordered prior output items; Arden previously kept only a derived tool-search call and omitted `tool_search_output`. Anthropic requires its native search blocks unchanged. | `apps/server/arden/llm/openai_responses.py:parse_responses_response,_convert_assistant_message`; `apps/server/tests/test_openai_client.py:test_responses_replays_exact_ordered_output_without_duplicates`; `apps/server/tests/test_reasoning_effort.py:test_anthropic_preserves_tool_search_blocks_for_next_request` | Provider-native wire state is opaque canonical history, not an ordinary result placeholder. | high | 2026-08-05 |
| F-20 | fact | Successful background-agent reports were injected into the parent without a bound even though the exact report already existed in `background_agent_runs.result_text`. | `apps/server/arden/tools/core/context.py:_bound_completed_result`; `apps/server/arden/tools/background.py:agent_result_read`; Hermes `tools/delegate_tool.py:1777-1942` | Parent notifications need a bounded head/tail and an exact durable paging tool. | high | 2026-08-05 |
| F-21 | fact | Search-result manifests expired after seven days while active `file_read` pointers can live indefinitely and session-local files are pruned after one day. | `apps/server/arden/context/store.py:prune_expired_tool_results`; `apps/server/arden/core/tool_result_files.py`; `apps/server/tests/test_session_store.py:test_active_search_result_pins_expired_manifest_until_context_drops_it` | Expiry must skip manifests referenced by the authoritative active projection. | high | 2026-08-05 |
| F-22 | fact | A short history can still exceed context because one opaque provider item is huge, but the old compactor refused histories of four or fewer messages. | `apps/server/arden/core/compactor.py:compactable_range`; `apps/server/tests/test_compactor.py:test_short_history_can_compact_opaque_provider_state` | Compaction eligibility must account for opaque provider state even when message count is small. | high | 2026-08-05 |
| F-23 | fact | Branch creation previously committed the target session before cloning durable result ownership, so a crash or missing background row could leave a dangling branch. | `apps/server/arden/context/store.py:create_session_branch`; `apps/server/arden/services/session.py:branch`; branch rollback/source-deletion tests | Snapshot and recovery-row cloning must share one transaction; cache files can be rebuilt afterward. | high | 2026-08-05 |
| F-24 | fact | Expiry treated invalid or non-array `sessions.messages` as an empty projection even though load recovery would use `session_messages`. | `apps/server/arden/context/store.py:prune_expired_tool_results`; `apps/server/tests/test_session_store.py:test_invalid_active_projection_fails_closed_when_pruning_tool_results` | Pruning must fail closed until the authoritative projection is valid again. | high | 2026-08-05 |
| F-25 | fact | Content-first offload could shrink the model-visible envelope below the universal bound before the exact envelope was persisted, losing continuation and source metadata on crash recovery. | `apps/server/arden/core/tool_executor.py:_maybe_offload`; `apps/server/tests/test_tools.py:test_durable_large_result_recovers_without_nested_serialization` | Persist readable `.txt` content separately while the durable blob and `.payload.json` retain the exact original envelope. | high | 2026-08-05 |

## Conflicts and gaps

- The persisted transcript proves final corrupted state, not the exact model-visible content on every intermediate request. Code-path analysis is required for that reconstruction.
- Claude Code and Hermes both have a dangling-stub edge case in their lighter request-pruning paths because dedup receipts can outlive model-visible content. Arden explicitly couples eviction to content-receipt downgrade.

## Tool-harness audit

| Finding | Invariant | Severity | Resolution |
| --- | --- | --- | --- |
| Request-only limiter overwrote canonical history | #4 stable refs, #6 bounded output | high | Explicit `history_messages` commit channel; ordinary request projections no longer mutate history. |
| Inline evicted results had no recovery path | #4 stable refs, #12 recovery | high | Persist on first eviction and include exact `file_read` path. |
| Successful unchanged reads duplicated full bodies | #5 high signal, #6 bounded output, #10 idempotency | medium | Return a compact unchanged receipt while prior content remains visible. |
| Generic no-progress fuse ignores successful loops | #22 eval-driven ergonomics | low/deferred | Re-evaluate after live post-fix evidence; do not globally deduplicate stateful calls. |
| Aggregate request limiter discards accumulated successful results | #5 high signal, #6 bounded output | high | Remove it; bound each oversized result once at ingestion. |
| Scheduled-loop window silently changes the model's history | #12 recovery, #21 workflow continuity | high | Remove the window; use explicit pre-run compaction only. |
| Resume trusts mutable active snapshot instead of a checkpoint-derived view | #4 stable refs, #12 recovery | high | Persist retained-tail IDs in the summary checkpoint and reconstruct from the transcript. |
| Native OpenAI search output was not replayed | #4 stable refs, #21 workflow continuity | high | Persist and replay exact ordered Responses output items; keep the compact receipt separate. |
| Child-agent completion could flood parent context | #6 bounded output, #12 recovery | high | Persist exact report first, inject at most 24k characters, expose paged `agent_result_read`. |
| Expired manifest could leave an active file pointer dangling | #4 stable refs, #12 recovery | high | Pin expired rows while the active projection references the tool call. |
| Branch could outlive its copied recovery records | #4 stable refs, #10 atomic/idempotent ownership | high | Commit snapshot and durable tool/background refs together; rebuild local caches after commit. |
| Content offload could discard structured continuation metadata | #4 stable refs, #6 bounded output, #12 recovery | high | Separate readable raw content from the exact durable result envelope. |
| Malformed projection made expiry assume no active refs | #4 stable refs, #12 recovery | high | Fail closed and retain the session's manifests until projection repair/recovery. |

## Supporting material

- None.
